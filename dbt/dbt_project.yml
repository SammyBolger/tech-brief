name: tech_brief
version: 0.1.0
config-version: 2
profile: tech_brief

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"
  - "logs"

models:
  tech_brief:
    staging:
      +materialized: view
    marts:
      +materialized: table
